apiVersion: v1
kind: Service
metadata:
  name: carts
  namespace: sock-shop
  annotations:
    prometheus.io/scrape: "true"
  labels:
    name: carts
spec:
  ports:
    # the port that this service should serve on
    - port: 80
      targetPort: 80
  selector:
    name: carts
